apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-jenkins-project.fullname" . }}-test-connection"
  labels:
    {{- include "helm-jenkins-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-jenkins-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
